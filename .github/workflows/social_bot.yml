name: Social Bot

run-name: ${{ github.event_name == 'repository_dispatch' && '📡 Cloudflare Feed-Update Trigger' || github.event_name == 'schedule' && '⏱️ Scheduled Run' || '👤 Manual Run' }}

on:
  schedule:
    - cron: '*/50 * * * *' # Runs every 50 minutes (can be disabled if using external trigger)
  workflow_dispatch: # Allows manual trigger
  repository_dispatch: # Allows external trigger (e.g., from Cloudflare Worker)
    types: [rss_feed_update]

# Ensure only one instance runs at a time to prevent duplicate posts
concurrency:
  group: social-bot
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      changes: ${{ steps.save_state.outputs.changes }}
    steps:
      - name: Show Trigger Info
        run: |
          echo "🚀 Workflow triggered by: ${{ github.event_name }}"
          if [ "${{ github.event_name }}" = "repository_dispatch" ]; then
            echo "📡 Triggered by Cloudflare Worker RSS Monitor"
            echo "🔔 Event type: ${{ github.event.action }}"
            echo "📋 Changed feeds: ${{ toJson(github.event.client_payload.feeds) }}"
            echo "⏰ Triggered at: ${{ github.event.client_payload.triggered_at }}"
          elif [ "${{ github.event_name }}" = "schedule" ]; then
            echo "⏱️ Triggered by scheduled cron job"
          else
            echo "👤 Manually triggered"
          fi

      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: pip install -r social_bot/requirements.txt

      - name: Run Social Bot
        env:
          BSKY_HANDLE: ${{ secrets.BSKY_HANDLE }}
          BSKY_PW: ${{ secrets.BSKY_PW }}
          MASTO_TOKEN: ${{ secrets.MASTO_TOKEN }}
          INDEXNOW_KEY: ${{ secrets.INDEXNOW_KEY }}
          RSS_FEED_URL: ${{ secrets.RSS_FEED_URL }}
        run: python social_bot/social_bot.py

      - name: Save State
        id: save_state
        run: |
          git config user.name "Bot"
          git config user.email "bot@github.com"
          git add social_bot/posted_articles.txt
          # "|| exit 0" prevents error if there are no changes to commit
          if git commit -m "Update posted articles [skip ci]"; then
            echo "changes=true" >> $GITHUB_OUTPUT
            git push
          else
            echo "changes=false" >> $GITHUB_OUTPUT
          fi

  trigger-backup:
    needs: build
    if: needs.build.outputs.changes == 'true'
    uses: ./.github/workflows/backup_bot.yml
    secrets: inherit
